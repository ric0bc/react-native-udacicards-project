import { StyleSheet } from 'react-native'

export const gStyles = StyleSheet.create({
  container: {
    flex: 1
  },
  btnPrimary: {
    marginTop: 40,
    width: 200,
    height: 40,
    borderWidth: 1,
    backgroundColor: '#000',
    borderRadius: 4,
    alignItems: 'center',
    justifyContent: 'center'
  }
})
